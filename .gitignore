.DS_Store
_build/
.mooncakes/
.moonagent/
.claude/
node_modules/
.wrangler/

# Release workflow scratch files
changeset-status.json
RELEASE_NOTES.md

# Deploy-time wrangler config with the D1 database_id injected (see
# scripts/prepare-wrangler-d1.mjs). Regenerated per deploy; the tracked
# cf/wrangler.jsonc keeps the placeholder so no account id is committed.
cf/wrangler.deploy.jsonc
