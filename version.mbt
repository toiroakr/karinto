///|
/// Engine version, kept in lockstep with `package.json` / `moon.mod` by
/// `scripts/sync-moon-version.mjs` (run via `npm run version-packages`).
/// Stamped into SARIF `tool.driver.version` by `sarif_report`.
pub const ENGINE_VERSION = "0.9.4"
